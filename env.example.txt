# Copy this file to .env and fill in your real values. Never commit .env.

# --- Server ---
PORT=3000
SESSION_SECRET=change-this-to-a-long-random-string

# --- Admin login (set your own) ---
ADMIN_USERNAME=admin
# Generate a bcrypt hash for your password (see README "Setting your admin password")
ADMIN_PASSWORD_HASH=

# --- Safaricom Daraja (M-Pesa) ---
# Get these from https://developer.safaricom.co.ke after creating an app
MPESA_ENV=sandbox
# sandbox or production
MPESA_CONSUMER_KEY=
MPESA_CONSUMER_SECRET=
MPESA_SHORTCODE=174379
# your Till/Paybill number in production; sandbox default shown
MPESA_PASSKEY=
# Daraja "Lipa Na M-Pesa Online" passkey
MPESA_CALLBACK_URL=https://your-deployed-domain.com/api/mpesa/callback
# must be a public HTTPS URL Safaricom can reach — won't work with localhost

# --- Email (sends the PDF ticket receipt) ---
EMAIL_USER=youraddress@gmail.com
EMAIL_APP_PASSWORD=
# a 16-character Gmail "App Password" — NOT your normal Gmail password.
# Generate one at https://myaccount.google.com/apppasswords
# (requires 2-Step Verification to be turned on for your Google account)
